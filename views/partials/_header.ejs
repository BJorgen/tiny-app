<head>
    <div>
        <h1>TinyApp</h1>
    </div>

    <% if(user){ %>

        
        <!-- If user is logged in, show logged in user & logout button-->
        <!-- Also, for logged in users, have link to urls sumamry and create new url page -->
        <div>
            <a href='/urls'> URLs</a> |
            <a href="/urls/new">Create New TinyURL</a>
        </div>

        <div class="form-group mb-2">
            <p>Logged in as: <b><%=user.email%></b> </p>
        </div>

        <form class="form-inline" action="/logout" method="POST">
        <div class="form-group mb-2">
            <button type="submit" name="logout-button" class="btn btn-danger">Logout</button>
        </div>
        </form>

    <% } else{ %>

        <!-- If user is not logged in, show login link and registration links -->
        <div>
            <a href='/login'> Login </a> |
            <a href='/register'> Register</a>
        </div>

    <% } %>


</head>

